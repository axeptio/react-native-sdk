pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'AxeptioSdkExample'

include(':app')

// Load React Native Gradle plugin from node_modules
includeBuild('../node_modules/@react-native/gradle-plugin')
